name: Deploy Production

on:
  push:
    branches:
    - master
  workflow_dispatch:
jobs:
  prod-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Production.
      run: |
        echo "Production"

   
