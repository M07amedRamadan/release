on:
  push:
    tags:
      - '*'

name: Auto Release Process_Do not Touch

jobs:
  # publish:
  #   name: Publish to npm
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '14'
  #         cache: 'npm'
  #     - name: Publish
  #       run: |
  #         npm config set //registry.npmjs.org/:_authToken ${NPM_TOKEN}
  #         npm ci
  #         npm publish
  #       env:
  #         NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

  create-github-release:
    name: Create GitHub Release
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      deployments: write
      contents: write
      statuses: write
    # needs: publish
    outputs: 
      RELEASE_ID: ${{ steps.release.outputs.releaseId}}

    steps:
  
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
           token: ${{ secrets.VERSION_UPDATE_WORKFLOW_TOKEN }}
      
      - name: Install GitHub CLI
        run: sudo apt-get install -y gh
   
      - name: Authenticate GitHub CLI
        run: gh auth login --with-token <<< ${{ secrets.VERSION_UPDATE_WORKFLOW_TOKEN }}
        
      - name: Extract version from branch name (for release branches)
        id: release
        run: |
          BRANCH_NAME="${{ github.ref }}"
          VERSION=${BRANCH_NAME#refs/tags/}
          echo "RELEASE_VERSION=$VERSION" >> $GITHUB_ENV

      - name: print the release ID
        run: |
            echo "the releaseID is= ${{ env.RELEASE_VERSION }} "
          
      - name: Create Release Notes
        uses: actions/github-script@v7
        with:
          github-token: ${{github.token}}
          script: |
              await github.request('POST /repos/${{ github.repository }}/releases', {
              tag_name: "${{ github.ref }}",
              name: "v${{ env.RELEASE_VERSION }} release",
              generate_release_notes: true
                });

      - name: add notes using GH-cli
        run: |
          gh release edit v${{ env.RELEASE_VERSION }} --notes release_notes.md
                
      # - name: Append Notes to Release
      #   uses: actions/github-script@v5
      #   with:
      #     script: |
      #        await github.request('POST /repos/${{ github.repository }}/releases/v${{ env.RELEASE_VERSION }}/update', {
      #           body: "Additional release notes here"
      #         });

 
